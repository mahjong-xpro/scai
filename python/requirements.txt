# Python 依赖包

# 深度学习框架
torch>=2.0.0
numpy>=1.24.0

# Rust 绑定（通过 PyO3）
# 需要先构建 Rust 扩展：cd rust && maturin develop

# 分布式训练
ray>=2.0.0  # 分布式并行框架

# 工具库
tqdm>=4.65.0  # 进度条
pyyaml>=6.0  # YAML 配置文件解析
tensorboard>=2.13.0  # 训练可视化
flask>=2.3.0  # Web 服务器
flask-cors>=4.0.0  # CORS 支持

# 注意：Coach 模块已改为文档生成模式，不再需要大模型 API 依赖
# 如需使用大模型分析，请手动将生成的文档提交给大模型

